#!/usr/bin/env python3
"""Entry point: `mahler <command>`. See `mahler --help`."""
import os
import sys

sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.realpath(__file__))))

from mahler.cli import main  # noqa: E402

sys.exit(main())
