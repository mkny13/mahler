"""The operator console (DESIGN D27, docs/console/design.md).

`state.build` turns the ledger into plain data, `page` lays it out for phone
and desktop, `actions` applies the console's writes. `mahler/serve.py` serves
all three.
"""
